// Firebase has been removed. This stub exists to avoid breaking imports during migration.
// All functionality has moved to src/config/api.config.ts and src/lib/auth.lib.ts.

export const projectId = "";
export const firebaseApp = null;
export const firebaseAuth = null as any;
export const firestore = null as any;
export const storage = null as any;
export const functions = null as any;
